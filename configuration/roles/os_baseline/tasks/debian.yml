---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600
  tags: [apt, update]

- name: Install baseline packages (Debian family)
  ansible.builtin.apt:
    name: "{{ baseline_packages }}"
    state: present
  tags: [packages]
